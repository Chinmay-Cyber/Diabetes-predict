# Diabetes-predict
This project builds a classification model using a Random Forest to predict diabetes outcomes by first loading and exploring the dataset with Pandas, visualizing feature distributions using Matplotlib and Seaborn, and preprocessing the data through a train-test split and feature scaling with StandardScaler; the trained model is then evaluated using a confusion matrix and classification report to assess prediction performance and interpret results.
